# Copy to keystore.properties and fill in to sign release builds locally.
# keystore.properties is gitignored. Never commit a real keystore or its passwords.
storeFile=/absolute/path/to/blokd.jks
storePassword=changeme
keyAlias=blokd
keyPassword=changeme
